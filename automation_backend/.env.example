# Backend environment example

# Flask secret (development default is fine)
SECRET_KEY=dev-secret-key

# DATABASE_URL: SQLAlchemy URL
# Example (MySQL):
# mysql+pymysql://USER:PASSWORD@HOST:PORT/DBNAME
# If not set, backend will attempt to parse automation_database/db_connection.txt once.
# Fallback if both missing: sqlite:///automation_dev.db
# DATABASE_URL=mysql+pymysql://user:pass@localhost:5001/automation

# Optional storage base (defaults to <repo>/automation_backend/storage)
# STORAGE_BASE=/data/automation_storage

# Pytest execution tuning
PYTEST_TIMEOUT=600
# Extra args passed to pytest (space separated), e.g. "-k smoke -x"
PYTEST_EXTRA_ARGS=
