# Backend environment example
# Copy to .env and adjust as needed. If DATABASE_URL is omitted,
# backend attempts to parse automation_database/db_connection.txt,
# otherwise falls back to local SQLite file automation_dev.db.

# Example MySQL URL:
# mysql+pymysql://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=

# Optional storage base (defaults to ./storage under backend directory)
# STORAGE_BASE=/absolute/or/relative/path

# Test execution tuning
# PYTEST_TIMEOUT=600
# PYTEST_EXTRA_ARGS=-q
