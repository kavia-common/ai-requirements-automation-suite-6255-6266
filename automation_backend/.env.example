# Backend environment example

# Database URL (use one of the following options)
# 1) MySQL (requires DB to exist):
# DATABASE_URL=mysql+pymysql://USER:PASSWORD@HOST:PORT/DBNAME
# 2) Or omit and backend will try reading automation_database/db_connection.txt
# 3) Or fallback to SQLite file (automation_dev.db)

# Optional storage base path. Defaults to ./storage under backend.
# STORAGE_BASE=

# Optional pytest execution settings
# PYTEST_TIMEOUT=600
# PYTEST_EXTRA_ARGS=
